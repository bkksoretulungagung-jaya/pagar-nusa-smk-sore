name: Install admin review compact v1

on:
  push:
    branches: [main]
    paths:
      - 'js/admin-review-compact-v1.js'
      - 'tools/install_admin_review_compact_v1.py'
      - '.github/workflows/install-admin-review-compact-v1.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install admin review compact
        run: python tools/install_admin_review_compact_v1.py
      - name: Commit installation
        run: |
          if git diff --quiet -- index.html; then
            echo "No changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Aktifkan accordion moderasi ulasan admin"
          git push
