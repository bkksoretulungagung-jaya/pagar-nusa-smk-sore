name: Install account create timeout fix v1

on:
  push:
    branches: [main]
    paths:
      - 'tools/install_account_create_timeout_fix_v1.py'
      - 'js/admin-account-create-timeout-fix-v1.js'
      - '.github/workflows/install-account-create-timeout-fix-v1.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install timeout fix
        run: python tools/install_account_create_timeout_fix_v1.py
      - name: Commit generated changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add js/admin-compact-v1.js index.html
          if git diff --cached --quiet; then
            echo "No generated changes"
          else
            git commit -m "Aktifkan fix timeout buat akun anggota"
            git push
          fi
